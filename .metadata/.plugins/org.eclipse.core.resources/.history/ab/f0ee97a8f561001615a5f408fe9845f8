/*
 * link_list.c
 *
 *  Created on: Aug 14, 2016
 *      Author: wenjian
 */
#include <stdlib.h>
#include "link_list.h"

struct LNode* create_link_list(int array[], int n){


	struct LNode* head = (struct LNode*)malloc(sizeof(struct LNode));

	struct LNode* p = head;
	int i = 0;
	printf("so are we here\n");
	for(i=0; i<n; i++){
		printf("so are we here\n");
		p->data = array[i];
		printf("so are we here\n");
		p->next = (struct LNode*)malloc(sizeof(struct LNode));
		printf("so are we here\n");
		p = p->next;
	}

	printf("so are we here\n");
	p->next = head;
	return head;
}
